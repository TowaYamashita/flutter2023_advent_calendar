import 'package:flutter/material.dart';

class Layout5BetterNoDataView extends StatelessWidget {
  const Layout5BetterNoDataView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TODOはありません'),
    );
  }
}
